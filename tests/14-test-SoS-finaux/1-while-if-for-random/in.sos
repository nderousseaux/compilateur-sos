i = 1;
j = 2;
sauss = 15;
declare tableau1[2];
tableau1[0] = 19;
tableau1[1] = 1;
if test ${i} -eq $(expr {j} -1) then
    declare tableau2[2];
    for id in ${tableau1[*]} do
        echo ${tableau1[*]}"\n"
    done ;
fi ;
baleine = "le sel";
echo ${baleine}" de baleine\n"
voyd = "";
tableau2[0] = ${baleine};
#la condition est vraie on rentre dans le while
while test -z ${voyd} do
    #on rnetre ici en DERNIER
    if ${expr ${i} + ${j}} -eq ${expr ${tableau1[0]} % 6} then  # si i+j = {19%6} = 1
        voyd = "je suis rempli";                                # on remplit voyd + on sort de la boucle
        echo ${voyd}"\n"
                                                                #on rentre en DEUXIEME ici
    elif ${expr ${j} * 2} -eq 8 then                            # si j*2 = 8
        i = ${expr ${i} - 4};    # i = 1 -4 = -3
        echo ${i}"\n"
                                                                #on rentre en PREMIER ici
    else                                                        # sinon
        j = ${expr ${j} * ${j}} # j prend la valeur 4;
        echo ${j}"\n"
    fi;
done;
echo "Fin du code Sos\n";
exit






